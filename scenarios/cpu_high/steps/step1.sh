## Step 1 – Detect CPU Issue

---

### `step3.md`
```md
## Step 3 – Fix the Issue

Stop Or kill the process by using kill command


#Run the following commands:

```bash
top   - Real-time monitoring of system performance and identifying resource-consuming processes.
-1.Now monitor for field name %CPU which is occupying more CPU% 
-2.Notedown those PIDs. 
-3.Then press Ctrl+c to exit
uptime - System load & how long it’s been running
mpstat - Per-CPU utilization statistics

CLICK NEXT
