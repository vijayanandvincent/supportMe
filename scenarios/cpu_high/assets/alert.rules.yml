groups:
- name: cpu-alerts
  rules:
  - alert: HighCPUUsage
    expr: (100 - avg(rate(node_cpu_seconds_total{mode="idle"}[1m])) * 100) > 85
    for: 1m
    labels:
      severity: warning
    annotations:
      summary: "CPU usage above 85%"
